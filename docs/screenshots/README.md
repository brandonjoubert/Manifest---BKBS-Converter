# Screenshots

Add product screenshots here for the main README.

Suggested captures:

1. `dashboard.png` — sites list / add site  
2. `entities.png` — entity review with status badges  
3. `settings-llm.png` — LLM provider settings  
4. `publish.png` — live publish / web root settings  

Then reference from the root README:

```markdown
![Dashboard](docs/screenshots/dashboard.png)
```

Keep images reasonably sized (&lt; 500 KB each) and avoid showing real API keys or private customer data.
